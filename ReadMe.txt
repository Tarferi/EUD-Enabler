# Starcraft EUD Enabler

This contains source code for EUD action enabler buildable as DLL and as BWL (chaos launcher plugin). It's most likely to be updated for masking support (which was added in remastered)